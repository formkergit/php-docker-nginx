# PHP Doccker

Personnaliser le nom de votre stack dans la variable `name` du `compose.yml`.

Lancer la stack  :  
`docker compose up -d --build`

Arrêter la stack  :  
`docker compose down`

Ensuite lancer :  
`http://localhost:8080`
